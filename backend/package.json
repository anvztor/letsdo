{
  "name": "express-framework",
  "version": "1.0.0",
  "description": "This is the template framework using express for the purpose of delivering features in a fast way. This framework should be maintained and updated or refactored if necessarily on regular basis to make sure this framework grows more robust and takes best practices from outside. It is a long term commitment.",
  "main": "index.js",
  "scripts": {
    "start": "export NODE_ENV=dev&&node ./bin/boostrap.js",
    "start_pro": "export NODE_ENV=production&&node ./bin/boostrap.js",
    "start_dev": "export NODE_ENV=dev&&node ./bin/boostrap.js",
    "start_test": "export NODE_ENV=test&&node ./bin/boostrap.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Justor",
  "license": "ISC",
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "hardhat": "^2.19.2",
    "ethers": "^6.4.0",
    "jsonwebtoken": "^9.0.1",
    "winston": "^3.10.0",
    "yup": "^1.2.0"
  }
}
